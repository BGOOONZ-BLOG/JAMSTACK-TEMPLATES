{
  "name": "stuff-nick-likes",
  "private": true,
  "repository": "johno/stuff-nick-likes",
  "description": "Stuff that Nick likes",
  "main": "index.js",
  "scripts": {
    "start": "x0 Stuff",
    "build": "x0 build Stuff -d dist",
    "deploy": "npm run build && cd dist && now && cd ..",
    "format": "prettier --no-semi --single-quote --write *.js",
    "test": "echo 'my code never needs tests, it is perfect'"
  },
  "devDependencies": {
    "@compositor/x0": "^4.0.1",
    "prettier": "^1.11.1"
  },
  "dependencies": {
    "random-a11y-combo": "^1.0.0",
    "react-typist": "^2.0.4"
  }
}
