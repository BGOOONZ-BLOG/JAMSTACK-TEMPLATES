import {
  dynamicRender
} from 'ember-dynamic-render/helpers/dynamic-render';

module('DynamicRenderHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dynamicRender(42);
  ok(result);
});
